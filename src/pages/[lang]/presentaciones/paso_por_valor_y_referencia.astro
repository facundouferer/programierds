---
import Layout from '../../../layouts/Layout.astro';
import { getRelativePath } from '../../../utils/paths';
import { useTranslations, type Locale } from '../../../i18n';
export async function getStaticPaths() { return [{ params: { lang: "es" } }, { params: { lang: "en" } }]; }
const { lang } = Astro.params as { lang: Locale };
---

<Layout title={lang === 'en' ? 'Pass by Value and Pointers in C - Programierds' : 'Paso por Valor y Punteros en C - Programierds'}>
	<style is:global>
		@import url('https://fonts.googleapis.com/css2?family=VT323&family=Space+Mono:wght@400;700&display=swap');

		:root {
			--primary: #0df259;
			--crt-black: #050a05;
			--crt-dark: #0a140a;
			--cyan: #22d3ee;
			--amber: #f59e0b;
			--orange: #fb923c;
		}

		html {
			scroll-behavior: smooth;
		}

		.crt-wrapper {
			background: radial-gradient(circle, #0a1a0a 0%, #050a05 100%);
			position: relative;
		}

		.crt-main {
			font-family: 'Space Mono', monospace;
			color: var(--primary);
			position: relative;
		}

		.font-retro {
			font-family: 'VT323', monospace;
		}

		/* Progress bar */
		#progress-bar {
			position: fixed;
			top: 0;
			left: 0;
			height: 3px;
			width: 0%;
			background: var(--primary);
			box-shadow: 0 0 10px var(--primary), 0 0 20px var(--primary);
			z-index: 200;
			transition: width 0.1s linear;
		}

		/* Sections */
		.section {
			min-height: 100vh;
			display: flex;
			align-items: center;
			justify-content: center;
			padding: 80px 24px 40px;
			position: relative;
			z-index: 10;
		}

		.section-inner {
			width: 100%;
			max-width: 1100px;
		}

		/* Reveal animations */
		.section-hidden {
			opacity: 0;
			transform: translateY(60px);
			transition: opacity 0.8s ease, transform 0.8s ease;
		}

		.section-visible {
			opacity: 1;
			transform: translateY(0);
		}

		.anim-item {
			opacity: 0;
			transform: translateY(30px);
			transition: opacity 0.6s ease, transform 0.6s ease;
			transition-delay: var(--delay, 0ms);
		}

		.section-visible .anim-item {
			opacity: 1;
			transform: translateY(0);
		}

		/* Divider */
		.section-divider {
			border: none;
			border-top: 1px solid rgba(13, 242, 89, 0.15);
			margin: 0 24px;
		}

		/* Code block */
		.code-block {
			background: rgba(0, 0, 0, 0.5);
			border: 1px solid rgba(13, 242, 89, 0.2);
			border-radius: 4px;
			padding: 16px 20px;
			font-family: 'Space Mono', monospace;
			font-size: 0.85rem;
			line-height: 1.7;
		}

		.code-comment { color: rgba(13, 242, 89, 0.4); font-style: italic; }
		.code-keyword { color: #61afef; }
		.code-string { color: #e5c07b; }
		.code-number { color: #d19a66; }
		.code-result { color: var(--primary); }
		.code-type { color: #f59e0b; }
		.code-class { color: #c678dd; }

		/* ==================== KEYFRAMES ==================== */

		@keyframes float {
			0%, 100% { transform: translateY(0px); }
			50% { transform: translateY(-12px); }
		}

		@keyframes float-slow {
			0%, 100% { transform: translateY(0px) rotate(0deg); }
			50% { transform: translateY(-18px) rotate(3deg); }
		}

		@keyframes blink-cursor {
			0%, 100% { opacity: 1; }
			50% { opacity: 0; }
		}

		@keyframes typing-sub {
			from { width: 0; opacity: 1; }
			to { width: 100%; opacity: 1; }
		}

		@keyframes glow-amber {
			0%, 100% { box-shadow: 0 0 8px rgba(245,158,11,0.4); }
			50% { box-shadow: 0 0 25px rgba(245,158,11,0.9), 0 0 50px rgba(245,158,11,0.5); }
		}

		@keyframes glow-cyan {
			0%, 100% { box-shadow: 0 0 8px rgba(34,211,238,0.4); }
			50% { box-shadow: 0 0 25px rgba(34,211,238,0.9), 0 0 50px rgba(34,211,238,0.5); }
		}

		@keyframes gold-pulse {
			0%, 100% {
				box-shadow: 0 0 15px rgba(245,158,11,0.6), 0 0 30px rgba(245,158,11,0.3);
				border-color: rgba(245,158,11,0.8);
			}
			50% {
				box-shadow: 0 0 40px rgba(245,158,11,1), 0 0 80px rgba(245,158,11,0.6);
				border-color: #f59e0b;
			}
		}

		@keyframes card-pop {
			from { opacity: 0; transform: translateY(40px) scale(0.9); }
			to { opacity: 1; transform: translateY(0) scale(1); }
		}

		/* ==================== SECTION 1: INTRO ==================== */

		.intro-title {
			font-family: 'VT323', monospace;
			font-size: clamp(2rem, 6vw, 5rem);
			text-shadow: 0 0 15px rgba(13, 242, 89, 0.7);
			line-height: 1;
		}

		.intro-subtitle {
			font-size: clamp(0.9rem, 2vw, 1.1rem);
			opacity: 0;
			overflow: hidden;
			white-space: nowrap;
			border-right: 2px solid var(--primary);
		}

		.intro-subtitle.typing-active {
			animation: typing-sub 3s steps(52) 0.5s forwards, blink-cursor 0.7s step-end infinite;
			width: 0;
		}

		.c-logo {
			font-family: 'VT323', monospace;
			font-size: 7rem;
			color: #f59e0b;
			text-shadow: 0 0 30px rgba(245,158,11,0.8);
			animation: float-slow 4s ease-in-out infinite;
			line-height: 1;
		}

		.intro-box {
			border: 2px solid;
			border-radius: 8px;
			padding: 20px 24px;
			text-align: center;
			min-width: 140px;
		}

		.intro-box-valor {
			border-color: rgba(245,158,11,0.6);
			background: rgba(245,158,11,0.06);
			animation: glow-amber 2.5s ease-in-out infinite;
		}

		.intro-box-ref {
			border-color: rgba(34,211,238,0.6);
			background: rgba(34,211,238,0.06);
			animation: glow-cyan 2.5s ease-in-out infinite;
			animation-delay: 1.2s;
		}

		/* ==================== MEMORY LAYOUTS ==================== */

		.memory-col {
			border: 2px solid;
			border-radius: 8px;
			padding: 20px;
			min-height: 300px;
			position: relative;
		}

		.stack-col {
			border-color: rgba(13,242,89,0.4);
			background: rgba(13,242,89,0.03);
		}

		.heap-col {
			border-color: rgba(34,211,238,0.4);
			background: rgba(34,211,238,0.03);
		}

		.memory-label {
			font-family: 'VT323', monospace;
			font-size: 1.5rem;
			letter-spacing: 0.1em;
			text-align: center;
			margin-bottom: 16px;
		}

		.stack-frame {
			border: 1px solid rgba(13,242,89,0.3);
			background: rgba(13,242,89,0.05);
			border-radius: 4px;
			padding: 8px 12px;
			margin-bottom: 8px;
			font-size: 0.8rem;
			display: flex;
			justify-content: space-between;
			align-items: center;
		}

		.stack-frame.prim-frame {
			border-color: rgba(245,158,11,0.4);
			background: rgba(245,158,11,0.05);
		}

		.stack-frame.ref-frame {
			border-color: rgba(34,211,238,0.4);
			background: rgba(34,211,238,0.05);
		}

		.heap-object {
			border: 2px solid rgba(13,242,89,0.4);
			border-radius: 6px;
			padding: 10px 14px;
			margin: 8px;
			font-size: 0.78rem;
			display: inline-block;
		}

		.heap-addr {
			font-size: 0.65rem;
			opacity: 0.4;
			font-family: 'Space Mono', monospace;
		}

		/* ==================== STEP ANIMATIONS ==================== */

		.anim-box {
			border: 2px solid;
			border-radius: 8px;
			padding: 20px 24px;
			text-align: center;
			min-width: 110px;
			transition: all 0.5s ease;
		}

		.anim-box-prim {
			border-color: rgba(245,158,11,0.5);
			background: rgba(245,158,11,0.05);
		}

		.anim-box-ref {
			border-color: rgba(34,211,238,0.5);
			background: rgba(34,211,238,0.05);
		}

		.anim-box-var {
			font-family: 'VT323', monospace;
			font-size: 0.9rem;
			opacity: 0.5;
			margin-bottom: 4px;
		}

		.anim-box-val {
			font-family: 'VT323', monospace;
			font-size: 2.5rem;
			line-height: 1;
			transition: color 0.5s ease;
		}

		.anim-box-val.changed {
			color: #ef4444;
		}

		.anim-arrow {
			display: flex;
			flex-direction: column;
			align-items: center;
			padding: 0 8px;
			opacity: 0;
			transition: opacity 0.5s ease;
		}

		.anim-arrow.visible {
			opacity: 1;
		}

		.step-result {
			opacity: 0;
			transition: opacity 0.6s ease;
		}

		.step-result.visible {
			opacity: 1;
		}

		.step-badge {
			display: inline-block;
			font-family: 'Space Mono', monospace;
			font-size: 0.7rem;
			padding: 2px 8px;
			border-radius: 3px;
		}

		.step-badge-ok {
			background: rgba(13,242,89,0.15);
			border: 1px solid rgba(13,242,89,0.4);
			color: var(--primary);
		}

		.step-badge-warn {
			background: rgba(245,158,11,0.15);
			border: 1px solid rgba(245,158,11,0.4);
			color: #f59e0b;
		}

		/* ==================== SECTION 9: REGLAS ==================== */

		.rule-card {
			border: 1px solid;
			border-radius: 8px;
			padding: 16px 20px;
			opacity: 0;
		}

		.rule-card.gold {
			border-color: rgba(245,158,11,0.7);
			background: rgba(245,158,11,0.06);
		}

		.rule-card.green {
			border-color: rgba(13,242,89,0.35);
			background: rgba(13,242,89,0.04);
		}

		.rule-card.cyan {
			border-color: rgba(34,211,238,0.35);
			background: rgba(34,211,238,0.04);
		}

		.rule-card.red {
			border-color: rgba(239,68,68,0.35);
			background: rgba(239,68,68,0.04);
		}

		.rule-card.pop-simple {
			animation: card-pop 0.5s ease forwards;
			animation-delay: var(--pop-delay, 0ms);
		}

		.rule-card.gold.pop-simple {
			animation: card-pop 0.5s ease forwards, gold-pulse 2s ease-in-out 0.5s infinite;
		}

		.rule-num {
			font-family: 'VT323', monospace;
			font-size: 3rem;
			line-height: 1;
			flex-shrink: 0;
		}

		/* ==================== TERMINAL STYLES ==================== */

		.terminal-btn {
			border: 1px solid var(--primary);
			padding: 8px 24px;
			text-transform: uppercase;
			font-size: 0.875rem;
			font-weight: 700;
			transition: all 0.3s;
			background-color: rgba(13, 242, 89, 0.05);
			color: var(--primary);
			text-decoration: none;
			display: inline-block;
			font-family: 'Space Mono', monospace;
			letter-spacing: 0.05em;
		}

		.terminal-btn:hover {
			background-color: var(--primary);
			color: black;
			box-shadow: 0 0 20px rgba(13, 242, 89, 0.5);
		}

		.split-col {
			border: 1px solid rgba(13, 242, 89, 0.2);
			border-radius: 8px;
			padding: 24px;
			background: rgba(13, 242, 89, 0.03);
		}

		.type-card {
			border: 1px solid rgba(13, 242, 89, 0.25);
			background: rgba(13, 242, 89, 0.04);
			border-radius: 6px;
			padding: 12px 16px;
			transition: border-color 0.3s, box-shadow 0.3s;
		}

		.type-card:hover {
			border-color: var(--primary);
			box-shadow: 0 0 12px rgba(13, 242, 89, 0.2);
		}

		/* Responsive */
		@media (max-width: 768px) {
			.split-grid { flex-direction: column; }
			.c-logo { font-size: 5rem; }
			.step-visual { flex-direction: column; align-items: center; }
		}
	</style>

	<div class="relative min-h-screen flex flex-col crt-wrapper">
		<!-- Progress bar -->
		<div id="progress-bar"></div>

		<div class="relative z-[200]">
		</div>

		<main class="flex-1 relative crt-main">

			<!-- ======================== SECTION 1: INTRO ======================== -->
			<section class="section section-hidden" id="sec-intro">
				<div class="section-inner text-center">
					<div class="anim-item" style="--delay: 0ms">
						<div class="text-xs tracking-widest opacity-40 mb-6 font-retro text-lg">PROGRAMIERDS_OS v1.0.4 // TOPIC: C_MEMORY</div>
					</div>

					<div class="anim-item mb-4" style="--delay: 100ms">
						<div class="c-logo">C</div>
					</div>

					<div class="anim-item" style="--delay: 200ms">
						<h1 class="intro-title uppercase tracking-tight mb-2">
							&gt; {lang === 'en' ? 'Pass by Value' : 'Paso por Valor'}
						</h1>
						<h1 class="intro-title uppercase tracking-tight mb-8" style="color: rgba(13,242,89,0.6)">
							{lang === 'en' ? 'and Reference' : 'y Referencia'}
						</h1>
					</div>

					<div class="anim-item flex justify-center mb-8" style="--delay: 400ms">
						<p class="intro-subtitle" id="intro-subtitle">
							{lang === 'en' ? 'In C, everything is passed by value. Pointers make it seem like magic, but they are still copies of addresses.' : 'En C, todo se pasa por valor. Los punteros hacen que parezca magia, pero siguen siendo copias de direcciones.'}
						</p>
					</div>

					<!-- Dos cajas intro -->
					<div class="anim-item flex justify-center gap-6 flex-wrap items-center" style="--delay: 550ms">
						<div class="intro-box intro-box-valor">
							<div class="font-retro text-3xl" style="color: #f59e0b">&#x2750;</div>
							<div class="font-retro text-xl mt-1" style="color: #f59e0b">{lang === 'en' ? 'VALUE' : 'VALOR'}</div>
							<div class="text-xs opacity-50 mt-1">{lang === 'en' ? 'direct copy' : 'copia directa'}</div>
						</div>
						<div class="font-retro text-3xl opacity-30">&#8596;</div>
						<div class="intro-box intro-box-ref">
							<div class="font-retro text-3xl" style="color: #22d3ee">&#x2794;</div>
							<div class="font-retro text-xl mt-1" style="color: #22d3ee">{lang === 'en' ? 'REFERENCE' : 'REFERENCIA'}</div>
							<div class="text-xs opacity-50 mt-1">{lang === 'en' ? 'memory address' : 'direcci&oacute;n de memoria'}</div>
						</div>
					</div>

					<div class="anim-item mt-12 opacity-50" style="--delay: 700ms">
						<p class="text-sm" style="animation: float 2s ease-in-out infinite">&#8595; {lang === 'en' ? 'scroll to continue' : 'scroll para continuar'}</p>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 2: STACK vs HEAP ======================== -->
			<section class="section section-hidden" id="sec-memoria">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest opacity-40 font-retro text-lg">{lang === 'en' ? 'FUNDAMENTAL' : 'FUNDAMENTO'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">Stack vs Heap</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'First understand how C organizes memory. Without this, nothing that follows makes sense.' : 'Primero entend&eacute; c&oacute;mo C organiza la memoria. Sin esto, nada de lo que viene tiene sentido.'}</p>
					</div>

					<div class="flex gap-6 split-grid">
						<!-- STACK -->
						<div class="flex-1 anim-item memory-col stack-col" style="--delay: 150ms">
							<div class="memory-label" style="color: var(--primary)">STACK</div>
							<p class="text-xs opacity-50 text-center mb-4">{lang === 'en' ? 'Local variables and references. LIFO.' : 'Variables locales y referencias. LIFO.'}</p>

							<div class="stack-frame prim-frame anim-item" style="--delay: 300ms">
								<span style="color: #f59e0b">int x</span>
								<span style="color: #f59e0b">42</span>
							</div>
							<div class="stack-frame prim-frame anim-item" style="--delay: 400ms">
								<span style="color: #f59e0b">boolean b</span>
								<span style="color: #f59e0b">true</span>
							</div>
							<div class="stack-frame ref-frame anim-item" style="--delay: 500ms">
								<span style="color: #22d3ee">int[] nums</span>
								<span style="color: #22d3ee" class="text-xs">&#x2192; 0x4A2F</span>
							</div>
							<div class="stack-frame ref-frame anim-item" style="--delay: 600ms">
								<span style="color: #22d3ee">Persona p</span>
								<span style="color: #22d3ee" class="text-xs">&#x2192; 0x8C1D</span>
							</div>

							<p class="text-xs opacity-40 mt-4 text-center">
								{lang === 'en' ? 'Primitives: the value lives here.' : 'Primitivos: el valor vive ac&aacute;.'}<br>
								{lang === 'en' ? 'Objects: only the address (reference).' : 'Objetos: solo la direcci&oacute;n (referencia).'}
							</p>
						</div>

						<!-- HEAP -->
						<div class="flex-1 anim-item memory-col heap-col" style="--delay: 300ms">
							<div class="memory-label" style="color: #22d3ee">HEAP</div>
							<p class="text-xs opacity-50 text-center mb-4">{lang === 'en' ? 'Real objects. Managed by the GC.' : 'Objetos reales. Gestionado por el GC.'}</p>

							<div class="flex flex-wrap justify-around">
								<div class="heap-object anim-item" style="--delay: 500ms">
									<div class="heap-addr">0x4A2F</div>
									<div class="font-retro text-xl" style="color: var(--primary)">int[ ]</div>
									<div class="text-xs mt-1 opacity-70">[ 1, 2, 3 ]</div>
								</div>
								<div class="heap-object anim-item" style="--delay: 650ms">
									<div class="heap-addr">0x8C1D</div>
									<div class="font-retro text-xl" style="color: #c678dd">Persona</div>
									<div class="text-xs mt-1 opacity-70">nombre: "Ana"</div>
								</div>
								<div class="heap-object anim-item" style="--delay: 800ms">
									<div class="heap-addr">0x3F0B</div>
									<div class="font-retro text-xl" style="color: #f59e0b">char *</div>
									<div class="text-xs mt-1 opacity-70">"Hola"</div>
								</div>
							</div>

							<p class="text-xs opacity-40 mt-4 text-center">
								{lang === 'en' ? 'Real objects live here.' : 'Los objetos reales viven ac&aacute;.'}<br>
								{lang === 'en' ? 'The Stack holds the address to reach them.' : 'El Stack guarda la direcci&oacute;n para llegar.'}
							</p>
						</div>
					</div>

					<div class="anim-item mt-6 type-card" style="--delay: 700ms">
						<p class="text-xs opacity-50 mb-2">{lang === 'en' ? 'THE FUNDAMENTAL RULE' : 'LA REGLA FUNDAMENTAL'}</p>
						<p class="text-sm">
							{lang === 'en' ? 'When C passes something to a function, what travels is the content of the variable on the Stack.' : 'Cuando C pasa algo a una funci&oacute;n, lo que viaja es el contenido de la variable en el Stack.'}
							{lang === 'en' ? 'For primitives, that is the' : 'Para primitivos eso es el'} <strong style="color: #f59e0b">{lang === 'en' ? 'direct value' : 'valor directo'}</strong>.
							{lang === 'en' ? 'For objects, that is the' : 'Para objetos eso es la'} <strong style="color: #22d3ee">{lang === 'en' ? 'memory address' : 'direcci&oacute;n de memoria'}</strong> ({lang === 'en' ? 'the reference' : 'la referencia'}).
							{lang === 'en' ? 'In BOTH cases: it is copied.' : 'En AMBOS casos: se copia.'}
						</p>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 3: PASO POR VALOR (primitivos) ======================== -->
			<section class="section section-hidden" id="sec-primitivos">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest opacity-40 font-retro text-lg">{lang === 'en' ? 'CASE 01' : 'CASO 01'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">{lang === 'en' ? 'Pass by Value' : 'Paso por Valor'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'With primitives (int, double, boolean, char...)' : 'Con primitivos (int, double, boolean, char...)'}</p>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-start">
						<!-- C&oacute;digo -->
						<div>
							<div class="anim-item code-block text-xs mb-4" style="--delay: 150ms">
								<span class="code-keyword">void</span> <span class="code-result">cambiar</span>(<span class="code-type">int</span> x) &lbrace;<br>
								&nbsp;&nbsp;x = <span class="code-number">99</span>;<br>
								&nbsp;&nbsp;<span class="code-comment">// x es una COPIA local</span><br>
								&rbrace;<br>
								<br>
								<span class="code-type">int</span> a = <span class="code-number">5</span>;<br>
								<span class="code-result">cambiar</span>(a);<br>
								printf("%d\n", a); <span class="code-comment">// 5 &larr; no cambi&oacute;</span>
							</div>

							<div class="anim-item type-card" style="--delay: 300ms">
								<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'WHY' : 'POR QU&Eacute;'}</p>
								<p class="text-sm opacity-80">
									{lang === 'en' ? 'C copies the value' : 'C copia el valor'} <span style="color: #f59e0b">5</span> {lang === 'en' ? 'into a new variable' : 'en una nueva variable'} <span style="color: #f59e0b">x</span>.
									{lang === 'en' ? 'Any change to' : 'Cualquier cambio a'} <code style="color: #f59e0b">x</code> {lang === 'en' ? 'is local to the method.' : 'es local al m&eacute;todo.'}
									<code style="color: var(--primary)">a</code> {lang === 'en' ? 'is never touched.' : 'nunca se toca.'}
								</p>
							</div>
						</div>

						<!-- Animaci&oacute;n paso a paso -->
						<div class="anim-item" style="--delay: 200ms">
							<p class="text-xs opacity-40 mb-4 tracking-widest text-center">{lang === 'en' ? 'VISUALIZATION' : 'VISUALIZACI&Oacute;N'}</p>

							<div class="flex justify-center gap-4 items-center step-visual" id="prim-visual">
								<!-- Caja a -->
								<div class="anim-box anim-box-prim" id="prim-box-a">
									<div class="anim-box-var">a</div>
									<div class="anim-box-val" id="prim-val-a" style="color: #f59e0b">5</div>
								</div>

								<!-- Flecha -->
								<div class="anim-arrow" id="prim-arrow">
									<span style="color: #f59e0b; font-family: 'VT323', monospace; font-size: 2rem">&#x2500;&#x2500;&#x2500;&gt;</span>
									<span style="color: #f59e0b; font-size: 0.6rem; margin-top: -8px">{lang === 'en' ? 'copy' : 'copia'}</span>
								</div>

								<!-- Caja x -->
								<div class="anim-box anim-box-prim" id="prim-box-x" style="opacity: 0">
									<div class="anim-box-var">{lang === 'en' ? 'x (copy)' : 'x (copia)'}</div>
									<div class="anim-box-val" id="prim-val-x" style="color: #f59e0b">5</div>
								</div>
							</div>

							<div class="step-result text-center mt-4" id="prim-step2">
								<p class="text-xs opacity-50 mb-2">{lang === 'en' ? 'x changes to 99...' : 'x cambia a 99...'}</p>
								<p class="text-sm opacity-80">{lang === 'en' ? '...but a is still' : '...pero a sigue siendo'} <span class="font-retro text-2xl" style="color: var(--primary)">5</span> &nbsp;<span style="color: var(--primary)">&#10003;</span></p>
							</div>
						</div>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 4: PASO POR VALOR (objetos) ======================== -->
			<section class="section section-hidden" id="sec-objetos">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest font-retro text-lg" style="color: #f59e0b">{lang === 'en' ? 'CASE 02 — THE TRICKY' : 'CASO 02 &mdash; EL TRICKY'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">{lang === 'en' ? 'Objects: The Reference is Copied' : 'Objetos: La Referencia se Copia'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'Both variables point to the SAME object in the heap' : 'Ambas variables apuntan al MISMO objeto en el heap'}</p>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-start">
						<div>
							<div class="anim-item code-block text-xs mb-4" style="--delay: 150ms">
								<span class="code-keyword">void</span> <span class="code-result">modificar</span>(<span class="code-type">int</span>[] arr) &lbrace;<br>
								&nbsp;&nbsp;arr[<span class="code-number">0</span>] = <span class="code-number">99</span>;<br>
								&nbsp;&nbsp;<span class="code-comment">// arr apunta al MISMO objeto</span><br>
								&rbrace;<br>
								<br>
								<span class="code-type">int</span>[] nums = &lbrace;<span class="code-number">1</span>,<span class="code-number">2</span>,<span class="code-number">3</span>&rbrace;;<br>
								<span class="code-result">modificar</span>(nums);<br>
								printf("%d\n", nums[<span class="code-number">0</span>]); <span class="code-comment">// 99!</span>
							</div>

							<div class="anim-item type-card" style="--delay: 300ms">
								<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'WHY' : 'POR QU&Eacute;'}</p>
								<p class="text-sm opacity-80">
									<code style="color: #22d3ee">arr</code> {lang === 'en' ? 'receives a COPY of the reference (the address' : 'recibe una COPIA de la referencia (la direcci&oacute;n'} <code>0x4A2F</code>).
									{lang === 'en' ? 'Both point to the same array in the heap.' : 'Ambas apuntan al mismo array en el heap.'}
									{lang === 'en' ? 'Modifying' : 'Modificar'} <code style="color: #22d3ee">arr[0]</code> {lang === 'en' ? 'modifies the real object, and' : 'modifica el objeto real, y'}
									<code style="color: var(--primary)">nums</code> {lang === 'en' ? 'sees it.' : 'lo ve.'}
								</p>
							</div>
						</div>

						<!-- Diagrama de referencias -->
						<div class="anim-item" style="--delay: 200ms">
							<p class="text-xs opacity-40 mb-4 tracking-widest text-center">{lang === 'en' ? 'MEMORY DIAGRAM' : 'DIAGRAMA DE MEMORIA'}</p>

							<div id="obj-visual" class="space-y-4">
								<!-- Stack -->
								<div class="memory-col stack-col" style="min-height: auto; padding: 12px 16px;">
									<div class="memory-label" style="font-size: 1rem; margin-bottom: 8px">STACK</div>
									<div class="flex gap-4 flex-wrap">
										<div class="stack-frame ref-frame flex-1" style="margin-bottom: 0">
											<span style="color: #22d3ee">nums</span>
											<span style="color: #22d3ee; font-size: 0.75rem">&#x2192; 0x4A2F</span>
										</div>
										<div class="stack-frame ref-frame flex-1" style="margin-bottom: 0; opacity: 0" id="obj-arr-frame">
											<span style="color: #22d3ee">{lang === 'en' ? 'arr (copy)' : 'arr (copia)'}</span>
											<span style="color: #22d3ee; font-size: 0.75rem">&#x2192; 0x4A2F</span>
										</div>
									</div>
								</div>

								<!-- Heap -->
								<div class="memory-col heap-col" style="min-height: auto; padding: 12px 16px;">
									<div class="memory-label" style="font-size: 1rem; margin-bottom: 8px">HEAP</div>
									<div class="heap-object" id="obj-heap-arr">
										<div class="heap-addr">0x4A2F</div>
										<div class="font-retro text-lg" style="color: var(--primary)" id="obj-heap-content">[ 1, 2, 3 ]</div>
									</div>
								</div>

								<div class="step-result text-center" id="obj-step-result">
									<span class="step-badge step-badge-warn">arr[0] = 99 &rarr; nums[0] == 99</span>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 5: LA TRAMPA ======================== -->
			<section class="section section-hidden" id="sec-trampa">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest font-retro text-lg" style="color: #ef4444">{lang === 'en' ? 'CASE 03 — THE TEST' : 'CASO 03 &mdash; LA PRUEBA'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight" style="color: #ef4444">{lang === 'en' ? 'Reassigning the Reference' : 'Reasignar la Referencia'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'This proves THAT C copies pointers by value' : 'Esto prueba QUE C copia los punteros por valor'}</p>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-start">
						<div>
							<div class="anim-item code-block text-xs mb-4" style="--delay: 150ms">
								<span class="code-keyword">void</span> <span class="code-result">reasignar</span>(<span class="code-type">int</span>[] arr) &lbrace;<br>
								&nbsp;&nbsp;arr = <span class="code-keyword">new</span> <span class="code-type">int</span>[]&lbrace;<span class="code-number">99</span>,<span class="code-number">99</span>,<span class="code-number">99</span>&rbrace;;<br>
								&nbsp;&nbsp;<span class="code-comment">// arr ahora apunta a OTRO objeto</span><br>
								&nbsp;&nbsp;<span class="code-comment">// nums NO se entera</span><br>
								&rbrace;<br>
								<br>
								<span class="code-type">int</span>[] nums = &lbrace;<span class="code-number">1</span>,<span class="code-number">2</span>,<span class="code-number">3</span>&rbrace;;<br>
								<span class="code-result">reasignar</span>(nums);<br>
								printf("%d\n", nums[<span class="code-number">0</span>]); <span class="code-comment">// 1 &larr; no cambi&oacute;</span>
							</div>

							<div class="anim-item type-card" style="--delay: 300ms">
								<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'THE KEY' : 'LA CLAVE'}</p>
								<p class="text-sm opacity-80">
									<code style="color: #ef4444">arr = new int[]...</code> {lang === 'en' ? 'only changes where the local COPY points.' : 'solo cambia a d&oacute;nde apunta la COPIA local.'}
									{lang === 'en' ? 'The original reference' : 'La referencia original'} <code style="color: var(--primary)">nums</code> {lang === 'en' ? 'in the caller\'s Stack' : 'en el Stack del llamador'}
									{lang === 'en' ? 'is not modified. It still points to the original object.' : 'no se modifica. Sigue apuntando al objeto original.'}
									<strong>{lang === 'en' ? 'This is the proof that in C pointers are also passed BY VALUE.' : 'Esta es la prueba de que en C los punteros tambi&eacute;n se pasan POR VALOR.'}</strong>
								</p>
							</div>
						</div>

						<!-- Diagrama trampa -->
						<div class="anim-item" style="--delay: 200ms">
							<p class="text-xs opacity-40 mb-4 tracking-widest text-center">{lang === 'en' ? 'DIAGRAM' : 'DIAGRAMA'}</p>

							<div id="trap-visual" class="space-y-4">
								<div class="memory-col stack-col" style="min-height: auto; padding: 12px 16px;">
									<div class="memory-label" style="font-size: 1rem; margin-bottom: 8px">STACK</div>
									<div class="flex gap-4 flex-wrap">
										<div class="stack-frame ref-frame flex-1" style="margin-bottom: 0">
											<span style="color: var(--primary)">nums</span>
											<span style="color: var(--primary); font-size: 0.75rem">&#x2192; 0x4A2F</span>
										</div>
										<div class="stack-frame flex-1" style="margin-bottom: 0; border-color: rgba(239,68,68,0.4); background: rgba(239,68,68,0.05); opacity: 0" id="trap-arr-frame">
											<span style="color: #ef4444">{lang === 'en' ? 'arr (copy)' : 'arr (copia)'}</span>
											<span style="color: #ef4444; font-size: 0.75rem" id="trap-arr-addr">&#x2192; 0x4A2F</span>
										</div>
									</div>
								</div>

								<div class="memory-col heap-col" style="min-height: auto; padding: 12px 16px;">
									<div class="memory-label" style="font-size: 1rem; margin-bottom: 8px">HEAP</div>
									<div class="flex gap-4 flex-wrap">
										<div class="heap-object">
											<div class="heap-addr">0x4A2F</div>
											<div class="font-retro text-lg" style="color: var(--primary)">[1, 2, 3]</div>
											<div class="text-xs opacity-50">&larr; {lang === 'en' ? 'nums points here' : 'nums apunta ac&aacute;'}</div>
										</div>
										<div class="heap-object" style="opacity: 0; border-color: rgba(239,68,68,0.4)" id="trap-new-obj">
											<div class="heap-addr">0x9E3C</div>
											<div class="font-retro text-lg" style="color: #ef4444">[99,99,99]</div>
											<div class="text-xs opacity-50" style="color: #ef4444">&larr; {lang === 'en' ? 'arr reassigned' : 'arr reasignado'}</div>
										</div>
									</div>
								</div>

								<div class="step-result text-center" id="trap-step-result">
									<span class="step-badge step-badge-ok">{lang === 'en' ? 'nums[0] is still 1' : 'nums[0] sigue siendo 1'}</span>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 6: OBJETOS CUSTOM ======================== -->
			<section class="section section-hidden" id="sec-persona">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest opacity-40 font-retro text-lg">{lang === 'en' ? 'CASE 04' : 'CASO 04'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">{lang === 'en' ? 'Person Class' : 'Clase Persona'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'Modifying fields vs reassigning the object' : 'Modificar campos vs reasignar el objeto'}</p>
					</div>

					<div class="flex gap-8 split-grid">
						<!-- Modificar campo: S&Iacute; afecta -->
						<div class="split-col flex-1 anim-item" style="--delay: 150ms">
							<h3 class="font-retro text-2xl mb-3" style="color: var(--primary)">&#10003; {lang === 'en' ? 'Modify the field' : 'Modifica el campo'}</h3>
							<div class="code-block text-xs mb-3">
								<span class="code-keyword">class</span> <span class="code-class">Persona</span> &lbrace;<br>
								&nbsp;&nbsp;<span class="code-type">char</span> nombre[<span class="code-number">20</span>];<br>
								&rbrace;<br>
								<br>
								<span class="code-keyword">void</span> <span class="code-result">cambiarNombre</span>(<span class="code-class">Persona</span> p) &lbrace;<br>
								&nbsp;&nbsp;p.nombre = <span class="code-string">"Nuevo"</span>;<br>
								&rbrace;<br>
								<br>
								<span class="code-class">Persona</span> ana = <span class="code-keyword">new</span> <span class="code-class">Persona</span>();<br>
								ana.nombre = <span class="code-string">"Ana"</span>;<br>
								<span class="code-result">cambiarNombre</span>(ana);<br>
								<span class="code-comment">// ana.nombre == "Nuevo" &#10003;</span>
							</div>
							<div class="type-card">
								<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'RESULT' : 'RESULTADO'}</p>
								<p class="text-sm"><code style="color: #22d3ee">p</code> {lang === 'en' ? 'points to the same object as' : 'apunta al mismo objeto que'} <code style="color: var(--primary)">ana</code>. {lang === 'en' ? 'Modifying' : 'Modificar'} <code style="color: #22d3ee">p.nombre</code> {lang === 'en' ? 'changes the shared object.' : 'cambia el objeto compartido.'}</p>
							</div>
						</div>

						<!-- Reasignar: NO afecta -->
						<div class="split-col flex-1 anim-item" style="--delay: 300ms">
							<h3 class="font-retro text-2xl mb-3" style="color: #ef4444">&#10007; {lang === 'en' ? 'Reassign the reference' : 'Reasigna la referencia'}</h3>
							<div class="code-block text-xs mb-3">
								<span class="code-keyword">void</span> <span class="code-result">reemplazar</span>(<span class="code-class">Persona</span> p) &lbrace;<br>
								&nbsp;&nbsp;p = <span class="code-keyword">new</span> <span class="code-class">Persona</span>();<br>
								&nbsp;&nbsp;p.nombre = <span class="code-string">"Otro"</span>;<br>
								&nbsp;&nbsp;<span class="code-comment">// p apunta a un objeto nuevo</span><br>
								&rbrace;<br>
								<br>
								<span class="code-class">Persona</span> ana = <span class="code-keyword">new</span> <span class="code-class">Persona</span>();<br>
								ana.nombre = <span class="code-string">"Ana"</span>;<br>
								<span class="code-result">reemplazar</span>(ana);<br>
								<span class="code-comment">// ana.nombre == "Ana" (sin cambios)</span>
							</div>
							<div class="type-card" style="border-color: rgba(239,68,68,0.3); background: rgba(239,68,68,0.04)">
								<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'RESULT' : 'RESULTADO'}</p>
								<p class="text-sm">{lang === 'en' ? 'The local copy' : 'La copia local'} <code style="color: #ef4444">p</code> {lang === 'en' ? 'points to another object.' : 'apunta a otro objeto.'} <code style="color: var(--primary)">ana</code> {lang === 'en' ? 'still points to the original.' : 'sigue apuntando al original.'}</p>
							</div>
						</div>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 7: STRINGS ======================== -->
			<section class="section section-hidden" id="sec-strings">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest font-retro text-lg" style="color: #f59e0b">{lang === 'en' ? 'SPECIAL CASE' : 'CASO ESPECIAL'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">{lang === 'en' ? 'Strings in C: Mutable Arrays' : 'Strings en C: arreglos mutables'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'In C, a char[] array can be modified character by character if you have writable memory.' : 'En C, un arreglo de char se puede modificar car&aacute;cter por car&aacute;cter si ten&eacute;s memoria escribible.'}</p>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-start">
						<div>
							<div class="anim-item code-block text-xs mb-4" style="--delay: 150ms">
								<span class="code-keyword">void</span> <span class="code-result">cambiar</span>(<span class="code-type">char</span> s[]) &lbrace;<br>
								&nbsp;&nbsp;s[<span class="code-number">0</span>] = <span class="code-string">'Z'</span>;<br>
								&nbsp;&nbsp;<span class="code-comment">// modifica el arreglo original</span><br>
								&rbrace;<br>
								<br>
								<span class="code-type">char</span> nombre[] = <span class="code-string">"Ana"</span>;<br>
								<span class="code-result">cambiar</span>(nombre);<br>
								printf("%s\n", nombre); <span class="code-comment">// "Zna"</span>
							</div>

							<div class="anim-item mb-4" style="--delay: 300ms">
								<div class="type-card mb-3">
									<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'IMMUTABILITY' : 'INMUTABILIDAD'}</p>
									<p class="text-sm opacity-80">
										<code style="color: #f59e0b">char[]</code> {lang === 'en' ? 'in C is mutable if the memory is writable.' : 'en C es mutable si la memoria es escribible.'}
										<code>s[0] = 'Z'</code> {lang === 'en' ? 'modifies the same received array.' : 'modifica el mismo arreglo recibido.'}
										{lang === 'en' ? 'The function writes directly on the same shared memory.' : 'La funci&oacute;n escribe directamente sobre la misma memoria compartida.'}
									</p>
								</div>
								<div class="type-card">
									<p class="text-xs opacity-50 mb-1">{lang === 'en' ? 'CHAR ARRAY' : 'ARREGLO DE CHARS'}</p>
									<p class="text-sm opacity-80">
										{lang === 'en' ? 'Literals are often read-only; if you want to mutate them, use your own char array.' : 'Los literales suelen ser de solo lectura; si quer&eacute;s mutarlos, us&aacute; un arreglo de chars propio.'}
										{lang === 'en' ? 'Use' : 'Us&aacute;'} <code>strcmp()</code> {lang === 'en' ? 'to compare content, don\'t compare addresses if you want to know if they say the same.' : 'para comparar contenido, no compares direcciones si quer&eacute;s saber si dicen lo mismo.'}
									</p>
								</div>
							</div>
						</div>

						<!-- Visual String immutability -->
						<div class="anim-item" style="--delay: 200ms">
							<p class="text-xs opacity-40 mb-4 tracking-widest text-center">{lang === 'en' ? 'WHAT HAPPENS IN MEMORY' : 'QU&Eacute; PASA EN MEMORIA'}</p>

							<div class="space-y-3">
								<div class="memory-col stack-col" style="min-height: auto; padding: 12px 16px;">
									<div class="memory-label" style="font-size: 1rem; margin-bottom: 8px">STACK</div>
									<div class="flex gap-4 flex-wrap">
										<div class="stack-frame ref-frame flex-1" style="margin-bottom: 0">
											<span style="color: var(--primary)">nombre</span>
											<span style="color: var(--primary); font-size: 0.75rem">&#x2192; 0xA1B2 &nbsp;&#10003;</span>
										</div>
										<div class="stack-frame flex-1" style="margin-bottom: 0; border-color: rgba(245,158,11,0.4); background: rgba(245,158,11,0.05)">
											<span style="color: #f59e0b">{lang === 'en' ? 's (copy)' : 's (copia)'}</span>
											<span style="color: #ef4444; font-size: 0.7rem">&#x2192; 0xC3D4 (new)</span>
										</div>
									</div>
								</div>

								<div class="memory-col heap-col" style="min-height: auto; padding: 12px 16px;">
									<div class="memory-label" style="font-size: 1rem; margin-bottom: 8px">HEAP</div>
									<div class="flex gap-4 flex-wrap">
										<div class="heap-object">
											<div class="heap-addr">0xA1B2 (buffer char[])</div>
											<div class="font-retro text-xl" style="color: var(--primary)">"Ana"</div>
											<div class="text-xs opacity-50">{lang === 'en' ? 'original, immutable' : 'original, inmutable'}</div>
										</div>
										<div class="heap-object" style="border-color: rgba(245,158,11,0.5)">
											<div class="heap-addr">0xC3D4</div>
											<div class="font-retro text-xl" style="color: #f59e0b">"Nuevo"</div>
											<div class="text-xs opacity-50">{lang === 'en' ? 'new object' : 'objeto nuevo'}</div>
										</div>
									</div>
								</div>

								<div class="text-center">
									<span class="step-badge step-badge-ok">{lang === 'en' ? 'nombre is still "Ana"' : 'nombre sigue siendo "Ana"'}</span>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 8: DIAGRAMA COMPLETO ======================== -->
			<section class="section section-hidden" id="sec-diagrama">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest opacity-40 font-retro text-lg">{lang === 'en' ? 'SNAPSHOT' : 'SNAPSHOT'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">{lang === 'en' ? 'Complete Memory Diagram' : 'Diagrama de Memoria Completo'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'Everything together. Primitives and references in action.' : 'Todo junto. Primitivos y referencias en acci&oacute;n.'}</p>
					</div>

					<div class="flex gap-6 split-grid">
						<!-- Stack completo -->
						<div class="flex-1 anim-item memory-col stack-col" style="--delay: 150ms">
							<div class="memory-label" style="color: var(--primary)">STACK</div>

							<div class="stack-frame prim-frame anim-item" style="--delay: 250ms">
								<span style="color: #f59e0b">int edad</span>
								<span style="color: #f59e0b">25</span>
							</div>
							<div class="stack-frame prim-frame anim-item" style="--delay: 350ms">
								<span style="color: #f59e0b">double sueldo</span>
								<span style="color: #f59e0b">3500.0</span>
							</div>
							<div class="stack-frame prim-frame anim-item" style="--delay: 450ms">
								<span style="color: #f59e0b">boolean activo</span>
								<span style="color: #f59e0b">true</span>
							</div>
							<div class="stack-frame ref-frame anim-item" style="--delay: 550ms">
								<span style="color: #22d3ee">char nombre[]</span>
								<span style="color: #22d3ee; font-size: 0.75rem">&#x2192; 0xA1B2</span>
							</div>
							<div class="stack-frame ref-frame anim-item" style="--delay: 650ms">
								<span style="color: #22d3ee">int[] scores</span>
								<span style="color: #22d3ee; font-size: 0.75rem">&#x2192; 0x4A2F</span>
							</div>
							<div class="stack-frame ref-frame anim-item" style="--delay: 750ms">
								<span style="color: #22d3ee">Persona p</span>
								<span style="color: #22d3ee; font-size: 0.75rem">&#x2192; 0x8C1D</span>
							</div>

							<div class="mt-4 text-xs opacity-30 text-center">
								<span style="color: #f59e0b">&#9632;</span> {lang === 'en' ? 'primitives: direct value' : 'primitivos: valor directo'} &nbsp;
								<span style="color: #22d3ee">&#9632;</span> {lang === 'en' ? 'objects: address' : 'objetos: direcci&oacute;n'}
							</div>
						</div>

						<!-- Heap completo -->
						<div class="flex-1 anim-item memory-col heap-col" style="--delay: 300ms">
							<div class="memory-label" style="color: #22d3ee">HEAP</div>

							<div class="flex flex-wrap gap-2">
								<div class="heap-object anim-item" style="--delay: 500ms">
									<div class="heap-addr">0xA1B2</div>
									<div class="font-retro text-lg" style="color: #f59e0b">char[]</div>
									<div class="text-xs opacity-70">"Ana"</div>
								</div>
								<div class="heap-object anim-item" style="--delay: 650ms">
									<div class="heap-addr">0x4A2F</div>
									<div class="font-retro text-lg" style="color: var(--primary)">int[]</div>
									<div class="text-xs opacity-70">[90, 85, 78]</div>
								</div>
								<div class="heap-object anim-item" style="--delay: 800ms">
									<div class="heap-addr">0x8C1D</div>
									<div class="font-retro text-lg" style="color: #c678dd">Persona</div>
									<div class="text-xs opacity-70">nombre: &#x2192; 0xA1B2</div>
									<div class="text-xs opacity-70">edad: 25</div>
								</div>
							</div>

							<p class="text-xs opacity-30 mt-4 text-center">
								{lang === 'en' ? 'Objects do not live in the Stack.' : 'Los objetos no viven en el Stack.'}<br>
								{lang === 'en' ? 'Only the address to find them.' : 'Solo la direcci&oacute;n para encontrarlos.'}
							</p>
						</div>
					</div>

					<div class="anim-item mt-6 type-card" style="--delay: 800ms">
						<p class="text-xs opacity-50 mb-2">{lang === 'en' ? 'NOTE ABOUT PRIMITIVES' : 'NOTA SOBRE PRIMITIVOS'}</p>
						<p class="text-sm opacity-80">
							{lang === 'en' ? 'Primitives (' : 'Los primitivos ('}<code style="color: #f59e0b">int</code>, <code style="color: #f59e0b">double</code>, <code style="color: #f59e0b">boolean</code>, <code style="color: #f59e0b">char</code>, <code style="color: #f59e0b">long</code>, <code style="color: #f59e0b">float</code>, <code style="color: #f59e0b">short</code>, <code style="color: #f59e0b">byte</code>)
							{lang === 'en' ? ' are the ONLY ones that live directly on the Stack. Everything else — including dynamic buffers, arrays allocated on the heap, and structs managed with pointers — lives on the Heap.' : ' son los &Uacute;NICOS que viven directamente en el Stack. Todo lo dem&aacute;s &mdash; incluyendo buffers din&aacute;micos, arrays reservados en heap y structs gestionados con punteros &mdash; vive en el Heap.'}
						</p>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 9: REGLAS DE ORO ======================== -->
			<section class="section section-hidden" id="sec-reglas">
				<div class="section-inner">
					<div class="anim-item text-center mb-10" style="--delay: 0ms">
						<span class="text-xs tracking-widest opacity-40 font-retro text-lg">{lang === 'en' ? 'SYNTHESIS' : 'S&Iacute;NTESIS'}</span>
						<h2 class="font-retro text-5xl uppercase tracking-tight">{lang === 'en' ? 'Golden Rules' : 'Reglas de Oro'}</h2>
						<p class="text-sm opacity-50 mt-2">{lang === 'en' ? 'Write them down. They are immutable.' : 'Grab&aacute;telas. Son inamovibles.'}</p>
					</div>

					<div class="space-y-4" id="rules-container">
						<!-- Regla 1 - gold -->
						<div class="rule-card gold" id="rule-1">
							<div class="flex items-center gap-4">
								<div class="rule-num" style="color: #f59e0b">01</div>
								<div>
									<p class="font-bold text-lg" style="color: #f59e0b">{lang === 'en' ? 'C ALWAYS passes by value' : 'C SIEMPRE pasa por valor'}</p>
									<p class="text-sm opacity-70 mt-1">{lang === 'en' ? 'No exceptions. Pointers don\'t change this rule: they just copy addresses.' : 'Sin excepciones. Los punteros no cambian esta regla: solo copian direcciones.'}</p>
								</div>
							</div>
						</div>

						<!-- Regla 2 -->
						<div class="rule-card green" id="rule-2">
							<div class="flex items-center gap-4">
								<div class="rule-num" style="color: var(--primary)">02</div>
								<div>
									<p class="font-bold" style="color: var(--primary)">{lang === 'en' ? 'For primitives: the VALUE is copied' : 'Para primitivos: se copia el VALOR'}</p>
									<p class="text-sm opacity-70 mt-1">{lang === 'en' ? 'The method receives a copy. Changing it does not affect the original.' : 'El m&eacute;todo recibe una copia. Cambiarla no afecta al original.'}</p>
								</div>
							</div>
						</div>

						<!-- Regla 3 -->
						<div class="rule-card cyan" id="rule-3">
							<div class="flex items-center gap-4">
								<div class="rule-num" style="color: #22d3ee">03</div>
								<div>
									<p class="font-bold" style="color: #22d3ee">{lang === 'en' ? 'For objects: the REFERENCE is copied' : 'Para objetos: se copia la REFERENCIA'}</p>
									<p class="text-sm opacity-70 mt-1">{lang === 'en' ? 'The copy contains the same memory address. Both point to the same object.' : 'La copia contiene la misma direcci&oacute;n de memoria. Ambas apuntan al mismo objeto.'}</p>
								</div>
							</div>
						</div>

						<!-- Regla 4 -->
						<div class="rule-card green" id="rule-4">
							<div class="flex items-center gap-4">
								<div class="rule-num" style="color: var(--primary)">04</div>
								<div>
									<p class="font-bold" style="color: var(--primary)">{lang === 'en' ? 'Modifying the object through the copy: DOES affect the original' : 'Modificar el objeto a trav&eacute;s de la copia: S&Iacute; afecta al original'}</p>
									<p class="text-sm opacity-70 mt-1"><code>arr[0] = 99</code>, <code>p.nombre = "X"</code> &mdash; {lang === 'en' ? 'both references see the change.' : 'ambas referencias ven el cambio.'}</p>
								</div>
							</div>
						</div>

						<!-- Regla 5 -->
						<div class="rule-card red" id="rule-5">
							<div class="flex items-center gap-4">
								<div class="rule-num" style="color: #ef4444">05</div>
								<div>
									<p class="font-bold" style="color: #ef4444">{lang === 'en' ? 'Reassigning the copied reference: does NOT affect the original' : 'Reasignar la referencia copiada: NO afecta al original'}</p>
									<p class="text-sm opacity-70 mt-1"><code>arr = new int[]...</code>, <code>p = new Persona()</code> &mdash; {lang === 'en' ? 'only moves the local copy.' : 'solo mueve la copia local.'}</p>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<hr class="section-divider" />

			<!-- ======================== SECTION 10: FIN ======================== -->
			<section class="section section-hidden" id="sec-fin">
				<div class="section-inner text-center">
					<div class="anim-item mb-6" style="--delay: 0ms">
						<div class="text-xs tracking-widest opacity-40 font-retro text-xl">ESTADO: SESION_COMPLETADA</div>
					</div>

					<div class="anim-item mb-4" style="--delay: 150ms">
						<h2 class="font-retro text-6xl uppercase tracking-tight" style="text-shadow: 0 0 30px rgba(13,242,89,0.7)">
							{lang === 'en' ? 'SESSION COMPLETED' : 'SESION FINALIZADA'}
						</h2>
					</div>

					<div class="anim-item mb-10" style="--delay: 300ms">
						<p class="text-sm opacity-50">
							{lang === 'en' ? 'Now you know the difference between pass by value and use of pointers in C.' : 'Ahora sab&eacute;s la diferencia entre paso por valor y uso de punteros en C.'}<br>
							{lang === 'en' ? 'Don\'t confuse it with "pass by reference" from other languages.' : 'No la vayas a confundir con "paso por referencia" de otros lenguajes.'}
						</p>
					</div>

					<div class="anim-item flex justify-center gap-6 mb-10 flex-wrap" style="--delay: 450ms">
						<div class="type-card text-left" style="min-width: 200px">
							<p class="text-xs opacity-50 mb-1" style="color: #f59e0b">{lang === 'en' ? 'PRIMITIVES' : 'PRIMITIVOS'}</p>
							<p class="text-sm opacity-80">int, double, boolean, char, long, float, short, byte</p>
							<p class="text-xs opacity-50 mt-1">&#x2192; {lang === 'en' ? 'copy of the value' : 'copia del valor'}</p>
						</div>
						<div class="type-card text-left" style="min-width: 200px; border-color: rgba(34,211,238,0.3); background: rgba(34,211,238,0.04)">
							<p class="text-xs opacity-50 mb-1" style="color: #22d3ee">{lang === 'en' ? 'OBJECTS' : 'OBJETOS'}</p>
							<p class="text-sm opacity-80">punteros, arrays, structs</p>
							<p class="text-xs opacity-50 mt-1">&#x2192; {lang === 'en' ? 'copy of the reference' : 'copia de la referencia'}</p>
						</div>
					</div>

					<div class="anim-item" style="--delay: 600ms">
						<p class="text-xs opacity-30 mb-6 tracking-widest">{lang === 'en' ? 'END OF SESSION // DATA PROCESSED CORRECTLY' : 'FIN DE SESION // DATOS PROCESADOS CORRECTAMENTE'}</p>
						<a href={getRelativePath('/presentaciones')} class="terminal-btn">
							&#8592; {lang === 'en' ? 'BACK TO PRESENTATIONS' : 'VOLVER A PRESENTACIONES'}
						</a>
					</div>
				</div>
			</section>

		</main>
	</div>

	<script>
		// ——— Progress bar ———
		const progressBar = document.getElementById('progress-bar') as HTMLElement;

		function updateProgress(): void {
			const scrollTop = window.scrollY;
			const docHeight = document.documentElement.scrollHeight - window.innerHeight;
			const progress = docHeight > 0 ? (scrollTop / docHeight) * 100 : 0;
			if (progressBar) progressBar.style.width = `${progress}%`;
		}

		window.addEventListener('scroll', updateProgress, { passive: true });
		updateProgress();

		// ——— IntersectionObserver for section reveals ———
		const sections = document.querySelectorAll('.section-hidden');

		const sectionObserver = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						const el = entry.target as HTMLElement;
						el.classList.add('section-visible');
						triggerSectionAnimations(el.id);
						sectionObserver.unobserve(el);
					}
				});
			},
			{ threshold: 0.2 }
		);

		sections.forEach((section) => sectionObserver.observe(section));

		// ——— Section-specific animations ———
		function triggerSectionAnimations(sectionId: string): void {
			switch (sectionId) {
				case 'sec-intro':
					triggerIntroTyping();
					break;
				case 'sec-primitivos':
					triggerPrimAnim();
					break;
				case 'sec-objetos':
					triggerObjAnim();
					break;
				case 'sec-trampa':
					triggerTrampaAnim();
					break;
				case 'sec-reglas':
					triggerRulesAnim();
					break;
			}
		}

		// INTRO: typing animation
		function triggerIntroTyping(): void {
			const el = document.getElementById('intro-subtitle') as HTMLElement | null;
			if (el) {
				setTimeout(() => {
					el.classList.add('typing-active');
				}, 600);
			}
		}

		// PRIMITIVOS: step by step animation
		function triggerPrimAnim(): void {
			const boxX = document.getElementById('prim-box-x') as HTMLElement | null;
			const arrow = document.getElementById('prim-arrow') as HTMLElement | null;
			const valX = document.getElementById('prim-val-x') as HTMLElement | null;
			const step2 = document.getElementById('prim-step2') as HTMLElement | null;

			setTimeout(() => {
				if (arrow) arrow.classList.add('visible');
			}, 600);

			setTimeout(() => {
				if (boxX) {
					boxX.style.opacity = '1';
					boxX.style.transition = 'opacity 0.5s ease';
				}
			}, 1000);

			setTimeout(() => {
				if (valX) {
					valX.textContent = '99';
					valX.classList.add('changed');
				}
			}, 2000);

			setTimeout(() => {
				if (step2) step2.classList.add('visible');
			}, 2800);
		}

		// OBJETOS: mostrar que arr se copia y ambas apuntan al mismo heap
		function triggerObjAnim(): void {
			const arrFrame = document.getElementById('obj-arr-frame') as HTMLElement | null;
			const heapContent = document.getElementById('obj-heap-content') as HTMLElement | null;
			const stepResult = document.getElementById('obj-step-result') as HTMLElement | null;

			setTimeout(() => {
				if (arrFrame) {
					arrFrame.style.opacity = '1';
					arrFrame.style.transition = 'opacity 0.5s ease';
				}
			}, 600);

			setTimeout(() => {
				if (heapContent) {
					heapContent.textContent = '[ 99, 2, 3 ]';
					heapContent.style.color = '#ef4444';
				}
			}, 1600);

			setTimeout(() => {
				if (stepResult) stepResult.classList.add('visible');
			}, 2400);
		}

		// TRAMPA: mostrar que arr se reasigna y nums no cambia
		function triggerTrampaAnim(): void {
			const arrFrame = document.getElementById('trap-arr-frame') as HTMLElement | null;
			const newObj = document.getElementById('trap-new-obj') as HTMLElement | null;
			const arrAddr = document.getElementById('trap-arr-addr') as HTMLElement | null;
			const stepResult = document.getElementById('trap-step-result') as HTMLElement | null;

			setTimeout(() => {
				if (arrFrame) {
					arrFrame.style.opacity = '1';
					arrFrame.style.transition = 'opacity 0.5s ease';
				}
			}, 600);

			setTimeout(() => {
				if (newObj) {
					newObj.style.opacity = '1';
					newObj.style.transition = 'opacity 0.5s ease';
				}
			}, 1400);

			setTimeout(() => {
				if (arrAddr) {
					arrAddr.textContent = '\u2192 0x9E3C (new)';
					arrAddr.style.color = '#ef4444';
				}
			}, 2200);

			setTimeout(() => {
				if (stepResult) stepResult.classList.add('visible');
			}, 3000);
		}

		// REGLAS: aparecen las cards una por una
		function triggerRulesAnim(): void {
			['rule-1', 'rule-2', 'rule-3', 'rule-4', 'rule-5'].forEach((id, i) => {
				setTimeout(() => {
					const el = document.getElementById(id) as HTMLElement | null;
					if (el) el.classList.add('pop-simple');
				}, i * 250);
			});
		}

		// Initial check for sections already visible on load
		sections.forEach((section) => {
			const rect = section.getBoundingClientRect();
			if (rect.top < window.innerHeight * 0.8) {
				const el = section as HTMLElement;
				el.classList.add('section-visible');
				triggerSectionAnimations(el.id);
				sectionObserver.unobserve(el);
			}
		});
	</script>
</Layout>